{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "start": "npm run install && npm run develop",
    "start:dev": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "npm i concurrently && cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "engines": {
    "node": "^14"
  }
}